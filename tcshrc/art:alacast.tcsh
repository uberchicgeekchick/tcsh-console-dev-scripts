#!/bin/tcsh -f
setenv PATH "${PATH}:/projects/gtk/alacast/bin:/projects/gtk/alacast/scripts";
setenv ALACAST_OPTIONS "--logging --titles-append-pubdate --player --strip-characters='!' --update=detailed --interactive";
#setenv ALACAST_OPTIONS "--logging --titles-append-pubdate --player --strip-characters='!'";
