#!/bin/tcsh -f
# Necassary evils

#I don't like python.  I like hearing it bitch even less
#'-W all' prints a warning for every occurance, would be useful if I felt python was.
#'-W default' prints one warning per occurance.  This is python's default behavior.
#'-W ignore': surpesses annoying warnings.
alias	python	"python -W ignore"

